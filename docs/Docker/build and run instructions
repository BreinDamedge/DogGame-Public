before you build this for a container, remove 'localhost' from main.py and replace w/empty string so that the port can be published

docker run -d --mount type=bind,src=<your_corpus_dir>/Documents,dst=/usr/local/app/Documents -p 1234:1234 --name DogGame doggame